# 🎨 Облепиха Images AI Bot

Telegram-бот для генерации изображений с использованием различных AI моделей (FLUX, ZImage, Imagen 4, Klein и др.) через сервис Pollinations.

Сделано с душой, при поддержке команды [Облепиха VPN](https://t.me/oblepiha_vpn_bot) 🧡

## ✨ Основные функции

- 🎨 **Генерация изображений** с помощью различных AI моделей
- 🤖 **Уточнение промтов** через Gemini AI (опционально)
- 📸 **Генерация по фото** - анализ изображения и создание на его основе
- ⚙️ **Настройки**:
  - Включение/выключение уточняющих вопросов
  - Выбор модели генерации
- 🎯 **Лимиты генераций** по моделям (ежедневные)
- 📊 **Админ-панель** с детальной аналитикой
- 🔐 **Проверка подписки** на канал и бота

## 🎭 Доступные модели

| Модель | Emoji | Дневной лимит |
|--------|-------|---------------|
| FLUX | ⚡ | Безлимит |
| ZImage | 🖼 | Безлимит |
| FLUX 2 Dev | 🔬 | 10 |
| Imagen 4 | 🌟 | 10 |
| Klein | 💎 | 7 |
| Klein Large | 👑 | 3 |
| GPT Image | 🤖 | 3 |

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Docker и Docker Compose (для контейнеризации)
- Telegram Bot Token
- API токен для сервиса генерации

### Локальный запуск

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/4iriks/ai-images-bot-for-oblepiha.git
cd ai-images-bot-for-oblepiha
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл
```

5. **Запустите бота:**
```bash
python bot.py
```

### Запуск через Docker

1. **Настройте .env файл** (см. раздел "Переменные окружения")

2. **Запустите контейнер:**
```bash
docker-compose up -d
```

3. **Просмотр логов:**
```bash
docker-compose logs -f
```

4. **Остановка:**
```bash
docker-compose down
```

## ⚙️ Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
# Обязательные параметры
BOT_TOKEN=your_telegram_bot_token_here
API_URL=http://localhost:3000
API_TOKEN=your_api_token_here

# Подписки (опционально)
REQUIRED_CHANNEL=@Oblepiha_Channel
REQUIRED_BOT=@oblepiha_vpn_bot

# Логирование
LOG_CHAT_ID=your_log_chat_id

# Администратор
ADMIN_ID=your_telegram_user_id

# База данных
DB_PATH=/app/data/bot.db  # для Docker
# DB_PATH=bot.db  # для локального запуска
```

### Описание параметров

- `BOT_TOKEN` - токен вашего Telegram бота от [@BotFather](https://t.me/BotFather)
- `API_URL` - URL сервиса генерации изображений
- `API_TOKEN` - токен для доступа к API сервиса
- `REQUIRED_CHANNEL` - канал для обязательной подписки (опционально)
- `REQUIRED_BOT` - бот для обязательного запуска (опционально)
- `LOG_CHAT_ID` - ID чата для отправки логов генераций
- `ADMIN_ID` - ваш Telegram User ID для доступа к админ-панели
- `DB_PATH` - путь к файлу базы данных SQLite

## 📁 Структура проекта

```
ai-images-bot-for-oblepiha/
├── bot.py                 # Точка входа приложения
├── config.py              # Конфигурация и настройки
├── requirements.txt       # Зависимости Python
├── Dockerfile            # Образ Docker
├── docker-compose.yml    # Конфигурация Docker Compose
├── .env.example          # Пример файла окружения
├── db/
│   ├── database.py       # Инициализация БД
│   └── models.py         # Модели и запросы к БД
├── handlers/
│   ├── start.py          # Обработка /start и подписок
│   ├── menu.py           # Главное меню и навигация
│   ├── settings.py       # Настройки пользователя
│   ├── generation.py     # Логика генерации изображений
│   └── admin.py          # Админ-панель
├── keyboards/
│   └── inline.py         # Inline клавиатуры
├── middlewares/
│   └── subscription.py   # Проверка подписок
├── services/
│   ├── pollinations.py   # Клиент API генерации
│   ├── gemini.py         # Gemini AI для промтов
│   └── logger.py         # Логирование в чат
├── states/
│   └── generation.py     # FSM состояния
└── utils/
    └── subscription.py   # Утилиты проверки подписок
```

## 📊 Админ-панель

Доступна по команде `/admin` (только для пользователя с ADMIN_ID).

**Метрики:**
- 👥 Общее количество пользователей
- 📅 Новые пользователи за день
- 🎨 Всего генераций
- 📊 Генераций сегодня
- 📈 Среднее количество промтов на пользователя
- 🔥 Самая популярная модель
- 🏆 Топ-7 пользователей сегодня
- 🎖 Топ-7 пользователей за все время

## 🛠 Разработка

### Добавление новой модели

1. Откройте `db/models.py`
2. Добавьте новую модель в словарь `MODELS`:
```python
MODELS = {
    # ...
    "new-model": {"name": "New Model", "emoji": "✨", "limit": 5},
}
```

### Изменение лимитов

Измените значение `limit` в словаре `MODELS` (0 = безлимит).

## 🐳 Docker

### Сборка образа
```bash
docker build -t ai-images-bot .
```

### Запуск контейнера
```bash
docker run -d \
  --name ai-images-bot \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  ai-images-bot
```

## 📝 Лицензия

Проект разработан для команды Облепиха.

## 🤝 Поддержка

- 🔐 [Облепиха VPN](https://t.me/oblepiha_vpn_bot)
- 📢 [Облепиха | новости](https://t.me/Oblepiha_Channel)

---

Сделано с 🧡 командой Облепиха
